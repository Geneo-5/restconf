- lire @README.md et continuer la feuille de route
- Mettre a jour @README.md en fonction des évolutions apporter

Pour construire le projet, utiliser la commande suivante a la racine du project : 
```sh
docker build -f docker/Dockerfile . -t restconfd:latest 
```
